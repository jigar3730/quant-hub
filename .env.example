# Required — no weak defaults. Generate a strong password for production.
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql://quant:change-me@localhost:5433/quant_hub

# Email notifications (quant-daily breakout + quant-swing weekly)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# EMAIL_FROM=
EMAIL_TO=you@example.com
